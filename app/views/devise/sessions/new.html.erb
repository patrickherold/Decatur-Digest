<h2 class="text-left">You must be signed in to access this info</h2>
<p>If don't have an account, sign up for FREE.<br><%= link_to "Create a FREE account", new_registration_path(resource_name), :class => "btn btn-success" %></p>
<p>OR you can create an account with one of these services:<br
<p><%- resource_class.omniauth_providers.each do |provider| %>
  <%= link_to "Sign in with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider), :class => "btn btn-info"  %><br /><br />
<% end -%></p>
<hr>
<h3>Sign in</h3>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email, :required => false, :autofocus => true %>
    <%= f.input :password, :required => false %>
    <%= f.input :remember_me, :class => "checkboxbutton", :as => :boolean if devise_mapping.rememberable? %>
  </div>
    <%= f.button :submit, "Sign in", :class => "btn-success btn-lg" %>
<% end %>
<hr>
<p>Other options</p>
<%= render "devise/shared/links" %>
